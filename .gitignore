node_modules/

# Salida de vite (renderer compilado) y de electron-builder (instalador)
dist/
release/

# Capturas de BEACON_CAPTURE y basura de sistema
*.png
!build/icon.png
Thumbs.db
.DS_Store

# Datos que los plugins de Claude Code dejan en el proyecto (no son de Beacon)
.claude/data/
